addEventListener





/*
let Matriz = [
    [0, 1, 2],
    [3, 4, 5],
    [6, 7, 8]
  ];
  
  let jugador = 1;

const matrizElement = document.getElementById('matriz');

matrizElement.addEventListener('click', function(event) {
  if (event.target.tagName === 'TD') {
    const contenidoCelda = event.target.textContent;
    if (contenidoCelda === '') {
      event.target.textContent = jugador === 1 ? 'X' : 'O';
      jugador = jugador === 1 ? 2 : 1;
    }
  }
});
*/
